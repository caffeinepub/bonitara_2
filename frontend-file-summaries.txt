{
  "frontend/src/pages/HomePage.tsx": "Full home page with hero slider, features bar, category grid, featured products grid, brand story, testimonials, and newsletter — imports `useCart`/`useWishlist` from `../App`, and correctly reads `ratingsMap` as `Map<number, number>` (plain number values, no `.averageRating` property).",
  "frontend/src/pages/CategoryPage.tsx": "Dynamic category page that accepts a `category` slug prop, imports `useCart`/`useWishlist` from `../App`, fetches all products and filters client-side to handle both new camelCase and legacy snake_case category strings, with sort controls and loading skeletons.",
  "frontend/src/pages/ProductDetailPage.tsx": "Product detail page that accepts a `productId: string` prop, imports `useCart`/`useWishlist` from `../App`, converts the string ID to a `number` for `useProductReviews`/`ReviewsList`/`ReviewForm`, and passes `BigInt` to the backend query.",
  "frontend/src/pages/CartPage.tsx": "Shopping cart page updated to use backend `Product` type — uses `imageUrl` instead of `image`, converts `bigint` price to `Number()` for arithmetic, and passes `bigint` IDs to `removeFromCart`/`updateQuantity`.",
  "frontend/src/pages/CheckoutPage.tsx": "Checkout page updated to use backend `Product` type — uses `imageUrl` instead of `image`, converts `bigint` price to `Number()` for arithmetic, and uses `product.id.toString()` as React keys.",
  "frontend/src/pages/WholesalePage.tsx": "Wholesale page with bulk pricing tiers, MOQ/GST info, feature highlights, and wholesale inquiry form with success state.",
  "frontend/src/pages/FAQPage.tsx": "FAQ page using shadcn Accordion component with 10 common questions and a contact CTA at the bottom.",
  "frontend/src/pages/ShippingPolicyPage.tsx": "Shipping policy page with info cards and detailed shipping sections — uses `shrink-0` instead of `flex-shrink-0`.",
  "frontend/src/pages/TermsPage.tsx": "Terms and conditions page with 8 sections covering website use, pricing, IP, disclaimers, liability, and governing law.",
  "frontend/src/pages/ReturnPolicyPage.tsx": "Return and refund policy page with info cards and detailed policy sections — uses `shrink-0` instead of `flex-shrink-0`.",
  "frontend/src/pages/ContactPage.tsx": "Contact page with business info cards, social links, map placeholder, and contact form — uses `shrink-0` instead of `flex-shrink-0`.",
  "frontend/src/pages/BlogListingPage.tsx": "Blog listing page displaying all blog posts as cards with title, excerpt, category, date, and image.",
  "frontend/src/pages/BlogPostPage.tsx": "Blog post detail page rendering full post content with tags and related posts section.",
  "frontend/src/pages/ProfilePage.tsx": "User profile page updated to use `wishlistItems` (array of `Product`) from the new `WishlistContext` instead of the old `items` property, removing the `useManualAuth` dependency and using Internet Identity only.",
  "frontend/src/pages/OrderHistoryPage.tsx": "Order history page with Internet Identity authentication guard showing a login prompt for unauthenticated users, and mock order list with status badges for authenticated users.",
  "frontend/src/pages/AdminDashboardPage.tsx": "Admin dashboard with overview stats, product management table (add/edit/delete/visibility toggle), orders, and inquiries tabs — uses a separate `AddProductFormErrors` interface to fix the type error on `errors.category`, calls `actor.adminAddProduct()` with the `Category` enum for new products.",
  "frontend/src/components/Footer.tsx": "Site footer with brand column, Quick Links, Policies (using correct `#/shipping-policy` and `#/return-policy` routes), and Contact columns — Categories column removed, grid adjusted to 4 columns, white/ivory text preserved.",
  "frontend/src/components/Header.tsx": "Sticky responsive header with logo, desktop/mobile navigation, cart/wishlist badges, and Internet Identity auth menu — admin link completely removed from both desktop dropdown and mobile drawer for all user states.",
  "frontend/src/components/ProductCard.tsx": "Product card component with smart image URL resolution and a `categoryToSlug` helper that maps both new camelCase (`candleMaking`, `soapMaking`, `fragrance`, `resinArt`) and legacy snake_case backend category strings to URL slugs, with wishlist toggle, add-to-cart, rating display, and out-of-stock badge.",
  "frontend/src/index.css": "Global styles with OKLCH color tokens, Cormorant Garamond/Jost fonts, scroll-behavior smooth on html, custom animation keyframes (fadeIn, slideUp, slideInRight, scaleIn), and styled scrollbar.",
  "frontend/src/App.tsx": "Main app component with hash-based routing, CartContext and WishlistContext providers — now correctly routes `/admin` and `/admin/login` to AdminLoginPage, `/admin/dashboard` to the protected AdminDashboardPage, and `isAdminRoute` covers all `/admin/*` paths to suppress the main layout.",
  "frontend/src/components/StarRating.tsx": "Reusable star rating component with two modes — `StarRatingDisplay` for showing a read-only rating with filled/empty stars, and `StarRatingSelector` for interactive star selection with hover effects. Supports sm/md/lg sizes using Lucide Star icons with amber fill for active stars.",
  "frontend/src/hooks/useProductReviews.ts": "Custom React Query hooks for product reviews — `useProductReviews(productId)` fetches reviews for a single product, and `useAllProductRatings()` fetches all product average ratings as a Map for efficient lookup in ProductCard.",
  "frontend/src/hooks/useSubmitReview.ts": "React Query mutation hook that submits a review to the backend via `actor.submitReview` and invalidates both `productReviews` and `allProductRatings` query caches on success.",
  "frontend/src/components/ReviewForm.tsx": "Review submission form with star rating selector, author name, optional title, and review body fields — includes client-side validation for all required fields, inline error display, loading spinner during submission, and success confirmation with option to write another review.",
  "frontend/src/components/ReviewsList.tsx": "Reviews list component that fetches and displays all reviews for a product including average rating summary, individual review cards with author, stars, date, title, and body. Shows empty state when no reviews exist.",
  "frontend/src/pages/WishlistPage.tsx": "New wishlist page that reads from WishlistContext to display saved products in a grid with remove buttons, add-to-cart actions, and an empty-state message with a link back to the homepage.",
  "frontend/src/pages/AdminLoginPage.tsx": "Admin login page that validates credentials via the backend `adminCheck` method (with a hardcoded fallback using the correct `Bonitara@2024` password), sets `adminSession` in sessionStorage on success, and redirects to `#/admin/dashboard`.",
  "frontend/src/components/AdminProtectedRoute.tsx": "Route guard that checks sessionStorage for `adminSession` and redirects unauthenticated users to `#/admin/login`; renders children only when authorized.",
  "frontend/src/pages/LoginPage.tsx": "Simplified login page using Internet Identity only — shows both a \"Sign In\" and \"Create Account\" button (both trigger the same Internet Identity flow which handles new and returning users), with no manual email/password form."
}